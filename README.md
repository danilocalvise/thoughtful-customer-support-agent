# thoughtful-customer-support-agent

Simple customer support agent UI that answers common Thoughtful AI questions
using a hardcoded dataset with a lightweight relevance matcher.

## Run locally

Open `index.html` in a browser.

Optional: serve locally for live reload.

```bash
python3 -m http.server 8000
```

Then visit `http://localhost:8000`.